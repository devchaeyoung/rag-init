# CLAUDE.md

## PR 리뷰 시 참고사항

- 보안 취약점이 있는 패키지를 새로 추가하는 PR이면 반드시 경고해주세요.
- 리뷰는 한국어로 작성해주세요.

## npm audit 보안 점검 PR 리뷰

PR 제목에 "보안 취약점 점검 보고서"가 포함되어 있거나 `audit-report.md` 파일이 변경된 PR을 리뷰할 때는 다음 형식으로 **조치 계획서**를 작성해주세요:

### 조치 계획서 형식

```
## 🛡️ 보안 취약점 조치 계획서

### 즉시 조치 (npm audit fix)
자동 수정 가능한 취약점을 나열하고 `npm audit fix` 명령으로 해결 가능한 항목을 정리

### Breaking Change 수반 조치
`--force` 필요한 항목에 대해:
- 영향 범위 분석 (어떤 기능에 영향을 주는지)
- 업그레이드 시 코드 변경 필요 여부
- 우선순위와 권장 일정

### 수정 불가 항목
자동 수정이 불가능한 취약점에 대해:
- 대안 패키지 제안
- 위험도 평가 (실제 공격 가능성)
- 임시 완화 조치

### 우선순위 요약
| 순위 | 패키지 | 조치 | 난이도 | 위험도 |
```

### 리뷰 기준
- Critical/High 취약점은 반드시 조치 방안을 제시
- 직접 의존성(dependencies)과 간접 의존성(devDependencies 하위)을 구분하여 분석
- devDependencies에만 있는 취약점은 프로덕션 영향이 없음을 명시
- 실제 공격 벡터가 해당 프로젝트에 적용 가능한지 판단
